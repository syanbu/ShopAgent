# Repository Instructions

开发产品功能或修改技术行为前，先阅读 `docs/README.md`，再读取其中映射的功能文档。新增用户能力、Agent 分支、API、数据结构或存储方案时，必须在同一次修改中创建或更新对应的 `docs/features/<feature>.md`，并更新文档索引。

不要执行任何 Git 命令或 Git 操作，包括查看状态、暂存、提交、建分支、合并、回退、推送和创建 PR。只有用户针对某一次操作明确授权后，才能执行该次 Git 操作。
